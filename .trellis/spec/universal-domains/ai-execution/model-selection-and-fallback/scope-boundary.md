# Scope Boundary

This concern covers:

* model choice based on task requirements
* fallback behavior when preferred models are unavailable
* degraded execution expectations and reporting

This concern does not cover:

* provider-specific API syntax
* prompt composition boundaries
* detailed evaluation methodology
