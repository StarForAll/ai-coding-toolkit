# Verification

Check the following:

* model selection matches task constraints
* fallback behavior is defined before failure occurs
* degraded execution is reported explicitly
* hidden model substitution does not invalidate the workflow
* no-result or escalation paths exist when fallback is insufficient
