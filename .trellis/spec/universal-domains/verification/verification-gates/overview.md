# Verification Gates

## Purpose

Define reusable verification gates that work must pass before merge, release, or formal completion.

## Applicability

Use this concern whenever a workflow needs auditable validation checkpoints rather than implicit confidence.
