# Verification

Check the following:

* command help describes the supported invocation shape
* required arguments or modes fail clearly when omitted
* successful execution returns a success exit code
* invalid invocation returns a non-success exit code
* wrapper or forwarding commands preserve underlying command behavior
