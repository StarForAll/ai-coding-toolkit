# Verification

Check the following:

* the project-level spec set explicitly states whether `README.md` is the default entry or another language policy applies
* if both `README.md` and `README.en.md` exist, they are treated as a paired set
* README changes update both language variants in the same change when the bilingual baseline applies
* both README variants keep visible cross-links near the top
* repository purpose, setup flow, structure, commands, and links do not drift between paired README variants
