# 审计并修复嵌入工作流中的 workflow-state 架构问题

## Goal

基于已经执行过 `trellis init` 且已嵌入目标工作流的临时项目 `/tmp/trellis-0.5.17-2`，判断 `docs/workflows/新项目开发工作流/` 当前产出的 `workflow-state.py` 及同类资产是否存在真实的可维护性/结构性问题；若问题真实存在，则只在 `docs/workflows/新项目开发工作流/` 范围内设计并实施修复，必要时通过该工作流自带补丁或安装器把修复带给后续目标项目。

## What I already know

* 用户要求以 `/tmp/trellis-0.5.17-2` 为证据来源分析，而不是把当前仓库自身运行态当作被审计对象。
* 修改范围严格限制在 `docs/workflows/新项目开发工作流/`；其他目录不能改，但当前任务目录可正常记录分析产物。
* 用户给出的候选问题是：`workflow-state.py` 单文件约 2734 行，承担状态查询、状态转换验证、门禁执行、修复逻辑、序列化等多重职责，怀疑已过度膨胀。
* 用户要求：先分析判断问题是否真实存在；若确认存在，先给出修正方案，待用户同意后再继续修改。
* `docs/workflows/新项目开发工作流/commands/workflow_assets.py` 当前 `COMPATIBLE_TRELLIS_VERSION = "0.5.17"`。
* 运行 `trellis -v` 得到 `0.5.17`，同版本门禁已通过。

## Assumptions (temporary)

* `/tmp/trellis-0.5.17-2` 内的嵌入结果足以反映当前工作流安装后的真实结构。
* 这次优先处理真实缺陷与高价值同类问题，不把“纯风格优化”误判成必须修复的问题。
* 如果 `workflow-state.py` 的问题本质来自安装器/补丁链路，则修复应落在源工作流对应脚本或补丁资产，而不是去改临时项目。

## Open Questions

* `workflow-state.py` 的膨胀是否已经造成真实维护风险、重复逻辑或补丁脆弱性，而不是单纯行数大？
* 是否存在同类的“单文件职责过载”脚本也需要一起拆分或至少为后续拆分铺路？
* 在不修改工作流外目录的前提下，最小且稳妥的修复切入点在哪里：直接模块化 `workflow-state.py`，还是先引入局部抽取与兼容包装层？

## Requirements (evolving)

* 以 `/tmp/trellis-0.5.17-2` 和源工作流脚本/文档作为证据，确认候选问题真假。
* 排查类似问题，避免只修单点而遗漏同类缺陷。
* 若确认问题存在，修复必须落在 `docs/workflows/新项目开发工作流/` 内。
* 修复不能引入新的安装/补丁/运行时兼容问题。
* 修改前先向用户汇报问题与方案，待用户同意后再实施。

## Acceptance Criteria (evolving)

* [ ] 输出基于证据的判断：候选问题是真问题、假问题，还是部分成立。
* [ ] 若存在真实问题，给出影响范围、根因、修复边界、相似问题清单。
* [ ] 用户同意后，修复仅修改 `docs/workflows/新项目开发工作流/` 下相关源资产。
* [ ] 修复后完成最相关的验证命令，并明确说明 `pass / fail / not run`。

## Definition of Done

* 结论与修复建议已写入任务产物，而不是只留在对话里
* 真实问题已修复或明确说明为什么暂不修
* 相关验证已执行并记录结果
* 若产出新的维护约束，后续评估是否需要补充到规范

## Out of Scope (explicit)

* 直接修改 `/tmp/trellis-0.5.17-2` 以外的非任务目录作为最终修复手段
* 变更当前仓库正在使用的 Trellis 主流程规则
* 没有证据支撑的“看起来更优雅”式重构

## Technical Notes

* 目标工作流根目录：`docs/workflows/新项目开发工作流/`
* 证据目标项目：`/tmp/trellis-0.5.17-2`
* 版本锚点：`0.5.17`
