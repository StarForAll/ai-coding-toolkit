# Desktop Platform Foundation

Selected assets:

* `spec.platforms.desktop-shared-application-runtime`
* `spec.platforms.desktop-shared-system-integration`

Use this example as a baseline for cross-platform desktop applications that need explicit runtime and system-integration boundaries before adding OS-specific rules.
