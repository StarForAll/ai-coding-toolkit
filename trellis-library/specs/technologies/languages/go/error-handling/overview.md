# Go Error Handling

## Purpose

Define reusable Go-specific rules for returning, wrapping, and checking errors so failure handling stays explicit and composable.

## Applicability

Use this concern when a project is implemented in Go and needs language-level guidance on error returns, wrapping, sentinel use, and boundary handling.
