# Verification

Check the following:

* function contracts expose meaningful error behavior
* wrapping preserves causality and adds useful meaning
* failure categories are not dependent on brittle string matching
* ignored returns and shallow pass-throughs are not normalized
* error conventions remain predictable across packages
