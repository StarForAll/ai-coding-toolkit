# Verification

Check the following:

* package layout makes import boundaries and ownership clear
* imports do not rely on path hacks or ambiguous execution context
* structure supports predictable installation and reuse
* domain code and entry points are easy to locate
* packaging conventions do not depend on hidden environment assumptions
