# Verification

Check the following:

* important interfaces are annotated clearly
* annotations improve readability and misuse detection
* type hints do not overstate runtime guarantees
* broad escape hatches remain explicit
* typing style is consistent enough to reveal weak spots
