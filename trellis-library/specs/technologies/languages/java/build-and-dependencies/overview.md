# Java Build and Dependencies

## Purpose

Define reusable Java ecosystem rules for dependency coordinates, version discipline, and build-facing dependency management so multi-module projects remain predictable and reviewable.

## Applicability

Use this concern when a Java project relies on Maven-style coordinates, shared dependency version management, or reusable internal libraries whose build and dependency contracts materially affect delivery risk.
