# Java Project Structure

## Purpose

Define reusable Java-specific rules for organizing packages, modules, and source boundaries so code remains discoverable and maintainable at scale.

## Applicability

Use this concern when a project is implemented in Java and needs language-level guidance on package structure, source layout, and ownership boundaries.
