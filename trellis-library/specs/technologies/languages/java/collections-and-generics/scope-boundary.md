# Scope Boundary

This concern covers Java collection semantics, generic variance, iteration discipline, and safe use of collection conversion helpers.

It does not replace domain-level data-model choices or framework-specific persistence/query collection behavior.
