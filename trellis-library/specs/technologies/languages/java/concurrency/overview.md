# Java Concurrency

## Purpose

Define reusable Java language-level rules for thread coordination, executor usage, lock discipline, and thread-local state so concurrent behavior remains reviewable and safe.

## Applicability

Use this concern when a Java project needs language-specific guidance on executors, locks, thread-local state, singleton safety, or shared-memory coordination.
