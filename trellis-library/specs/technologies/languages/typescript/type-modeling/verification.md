# Verification

Check the following:

* types reflect real invariants and boundaries
* public types are precise enough without becoming unusable
* unsound escape hatches are narrow and explicit
* nullable, optional, and stateful distinctions are modeled where relevant
* type structure still matches contract meaning
