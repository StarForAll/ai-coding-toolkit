# TypeScript Runtime Boundaries

## Purpose

Define reusable TypeScript-specific rules for separating compile-time type confidence from actual runtime trust boundaries.

## Applicability

Use this concern when a project is implemented in TypeScript and values from APIs, storage, forms, events, or untyped modules cross runtime boundaries.
