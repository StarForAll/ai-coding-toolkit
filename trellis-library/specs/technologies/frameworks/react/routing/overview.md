# React Routing

## Purpose

Define reusable React-specific rules for route-driven composition, navigation state, and screen ownership in React applications.

## Applicability

Use this concern when a React project uses route-based screens, nested navigation, parameterized pages, or route-level data and transition behavior.
