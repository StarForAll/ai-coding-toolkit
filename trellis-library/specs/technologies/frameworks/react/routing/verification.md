# Verification

Check the following:

* routing preserves clear boundaries between navigation and shared concerns
* route params, search state, and loading behavior are explicit
* guards, redirects, and transitions do not hide important user flow
* nested routes improve composition clarity
* new screens inherit predictable routing behavior
