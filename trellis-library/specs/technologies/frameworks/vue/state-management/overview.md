# Vue State Management

## Purpose

Define reusable Vue-specific rules for placing, sharing, and evolving state so reactive data flow remains understandable and stable.

## Applicability

Use this concern when a Vue project manages shared UI state, remote data state, workflow state, or cross-component coordination beyond trivial local component data.
