# Verification

Check the following:

* component responsibilities are clear
* props, emits, and public interfaces are understandable
* local state is not silently replacing broader state boundaries
* reusable components are not fake-generic wrappers around one-off behavior
* similar UI behaviors are modeled consistently
