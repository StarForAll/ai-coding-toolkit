# Config Change Safety

## Purpose

Define reusable rules for changing runtime configuration without understating blast radius, environment drift, or rollback difficulty.

## Applicability

Use this concern when modifying feature flags, environment variables, service configuration, rate limits, toggles, or operational settings that affect live behavior.
