# Verification

Check the following:

* configuration changes are treated as runtime behavior changes where appropriate
* affected environments, defaults, and assumptions are explicit
* rollback or disablement paths are available for meaningful risk
* hidden config-to-code coupling was considered
* post-rollout configuration state is observable enough to trust
