# Verification

Check the following:

* stable, project, task, and temporary context are distinguishable
* no lower-priority layer silently overrides a higher-priority layer
* transient notes are not treated as stable truth
* summaries can be traced back to their source layer
* injected context preserves boundary clarity
