# Verification

Check the following:

* externally visible failures have stable semantics
* consumers can distinguish category and response expectations
* adjacent boundaries do not expose arbitrary error-shape drift
* actionable detail is present without leaking sensitive internals
* uncertainty or partial failure states are represented honestly
