# Caching And Consistency

## Purpose

Define reusable rules for using caches without losing track of freshness, invalidation, and consistency boundaries.

## Applicability

Use this concern when systems rely on cached reads, derived state, replica lag, or other behavior where freshness and consistency tradeoffs affect correctness.
