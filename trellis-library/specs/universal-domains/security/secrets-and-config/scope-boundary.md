# Scope Boundary

This concern covers:

* secret versus non-secret configuration boundaries
* handling of environment-specific settings
* exposure and storage constraints for sensitive configuration

This concern does not cover:

* platform-specific secret manager integration
* deployment pipeline implementation details
* business approval policy for secret rotation
