# Verification

Check the following:

* environment-variable and session assumptions are explicit
* writable paths follow Linux user-environment conventions
* display-stack or session capability assumptions are not overgeneralized
* missing or incompatible runtime conditions degrade predictably
* debugging and support can recover the relevant environment state
