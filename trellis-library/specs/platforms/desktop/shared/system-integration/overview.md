# Desktop System Integration

## Purpose

Define reusable desktop-platform rules for application behavior that crosses into
operating-system services such as local file dialogs, shell integration,
notifications, desktop permissions, and other user-environment APIs.

## Applicability

Use this concern when a desktop application depends on OS-provided capabilities
whose availability, permissions, or interaction model can affect correctness or
user expectations.
