# Scope Boundary

This concern covers Windows-specific filesystem and path behavior such as path
construction, writable-location assumptions, install-directory constraints, and
file access semantics.

It does not replace cross-platform desktop runtime rules, application-specific
storage policy, or build-tool-specific packaging configuration.
