# Verification

Check the following:

* interaction does not depend only on pointer precision or visual cues
* semantic structure and state changes are exposed clearly
* keyboard and focus behavior are treated as real interaction paths
* accessibility was considered during behavior design, not only after visual implementation
* remaining accessibility gaps are visible
