# Verification

Check the following:

* browser execution constraints are reflected in the design
* important runtime assumptions are explicit
* browser-only behavior is not overgeneralized
* client runtime failure degrades predictably
* framework abstractions are not hiding web-platform constraints
