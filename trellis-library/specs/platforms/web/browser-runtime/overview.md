# Browser Runtime

## Purpose

Define reusable web-platform rules for behavior that depends on browser execution constraints, user agents, and client-side runtime limitations.

## Applicability

Use this concern when software runs in a web browser and behavior is affected by client runtime capabilities, execution order, or browser-environment differences.
