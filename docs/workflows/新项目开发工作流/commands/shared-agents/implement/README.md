# Implement

Shared workflow-local source asset for the `implement` role in the implementation-internal subagent chain.

## Purpose

Provide the canonical role definition used to generate per-CLI implement-agent
files for Claude, OpenCode, and Codex within this workflow.

## When To Use

- After research is complete
- When implementation work is authorized
- Before the internal `check` role runs

## Output

- Scoped code changes
- Verification results
- Remaining risks or follow-ups
