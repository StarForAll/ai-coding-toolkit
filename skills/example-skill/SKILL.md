---
name: example-skill
description: Minimal example skill template compatible with the Skills CLI (`npx skills add`).
metadata:
  author: your-name
  version: "0.1.0"
---

# Example Skill

This is a minimal, shareable skill.

## When to Use

Use this skill when the user asks for an example of how to structure a `SKILL.md` so it can be installed via `npx skills add`.

## What to Do

- Follow the repository structure rules.
- Ensure the YAML block at the top has `name` and `description` as strings.
- Keep the remaining content as plain Markdown instructions.
